---
phase: 02-java-course-audit
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - content/courses/java/modules/10-web-fundamentals-and-apis/**/content/*.md
  - content/courses/java/modules/10-web-fundamentals-and-apis/**/challenges/**
  - content/courses/java/modules/11-spring-boot/**/content/*.md
  - content/courses/java/modules/11-spring-boot/**/challenges/**
  - content/courses/java/modules/11-spring-boot/module.json
  - content/courses/java/modules/12-security-sessions-jwt/**/content/*.md
  - content/courses/java/modules/12-security-sessions-jwt/**/challenges/**
autonomous: true

must_haves:
  truths:
    - "All Spring Boot references in Modules 10-12 target version 4.0.x (not 3.4)"
    - "Module 11 teaches Spring Boot 4.0.x patterns: Spring Framework 7, Jakarta EE 11, virtual threads default"
    - "Module 12 Spring Security config uses Spring Boot 4.0.x compatible patterns"
    - "Every content file in Modules 10-12 uses IO.println exclusively"
    - "Docker images reference eclipse-temurin:25 (not 21)"
  artifacts:
    - path: "content/courses/java/modules/11-spring-boot/module.json"
      provides: "Spring Boot module metadata updated to 4.0.x"
      contains: "4.0"
    - path: "content/courses/java/modules/11-spring-boot/lessons/01-lesson-111-why-spring-boot/"
      provides: "Updated intro to Spring Boot 4.0.x"
      contains: "Spring Boot 4"
  key_links:
    - from: "Module 11 Spring Boot content"
      to: "Spring Boot 4.0.x APIs"
      via: "Version-consistent configuration and annotations"
      pattern: "Spring Boot 4"
    - from: "Module 12 Security content"
      to: "Spring Security (Spring Boot 4.0.x)"
      via: "Security filter chain configuration"
      pattern: "SecurityFilterChain"
---

<objective>
Migrate Modules 10-12 (Web/APIs, Spring Boot, Security/JWT) to target Spring Boot 4.0.x and Java 25. These modules form the web development core of the course and require the most substantial content updates due to the Spring Boot version change.

Purpose: Spring Boot is the centerpiece of the Java course's backend development path. Modules 10-12 contain 15 lessons and 20 challenges that currently reference Spring Boot 3.4. The upgrade to 4.0.x requires updating imports (Jakarta EE 11), annotations (@MockitoBean), configuration patterns, and version references throughout.

Output: All content and challenge files in Modules 10-12 updated to Spring Boot 4.0.x / Java 25 standards.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-java-course-audit/02-CONTEXT.md
@.planning/phases/02-java-course-audit/02-RESEARCH.md
@.planning/phases/02-java-course-audit/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Module 10 (Web/APIs) and Module 11 (Spring Boot) to Spring Boot 4.0.x</name>
  <files>
    content/courses/java/modules/10-web-fundamentals-and-apis/**/content/*.md
    content/courses/java/modules/10-web-fundamentals-and-apis/**/challenges/**
    content/courses/java/modules/11-spring-boot/**/content/*.md
    content/courses/java/modules/11-spring-boot/**/challenges/**
    content/courses/java/modules/11-spring-boot/module.json
  </files>
  <action>
    Module 10 has 3 lessons, 3 challenges. Module 11 has 7 lessons, 7 challenges.

    **Module 10 (Web Fundamentals and APIs):**

    For all 3 lessons:
    1. Replace System.out.println with IO.println
    2. Lesson on HttpClient: verify java.net.http.HttpClient patterns are current for Java 25
    3. REST APIs lesson: if it foreshadows Spring Boot, update references to "Spring Boot 4.0"
    4. Update any "Java 21" or "Spring Boot 3.4" version references

    For all 3 challenges:
    - Update solutions: IO.println, current HttpClient patterns

    **Module 11 (Spring Boot) -- MAJOR UPDATE:**

    This module needs the most substantial content changes because Spring Boot version affects everything.

    **Lesson 01 (Why Spring Boot):**
    - Update all "Spring Boot 3.4" references to "Spring Boot 4.0"
    - Update feature highlights: virtual threads enabled by default, Spring Framework 7, Jakarta EE 11
    - If content mentions Spring Initializr, note to select Spring Boot 4.0.x
    - Replace @MockBean mentions with @MockitoBean
    - Replace System.out.println with IO.println

    **Lesson 02 (REST Controllers):**
    - Verify @RestController, @GetMapping, @PostMapping patterns (these are the same in SB 4.0)
    - Update any Spring Boot version references
    - Replace System.out.println with IO.println

    **Lesson 03 (Request/Response):**
    - Verify request/response handling patterns for Spring Boot 4.0
    - Jackson 3.x ships with Spring Boot 4.0 -- if content configures Jackson, verify patterns are current
    - Replace System.out.println with IO.println

    **Lesson 04 (Dependency Injection):**
    - Core DI patterns are the same in Spring Boot 4.0
    - Update version references
    - Replace System.out.println with IO.println

    **Lesson 05 (Configuration):**
    - Update application.properties/application.yml examples for Spring Boot 4.0.x
    - Note: virtual threads are ON by default in Spring Boot 4.0 (no need to set `spring.threads.virtual.enabled=true`)
    - If content shows enabling virtual threads manually, update to note this is the DEFAULT
    - Replace System.out.println with IO.println

    **Lesson 06 (Exception Handling):**
    - Verify @ExceptionHandler and @ControllerAdvice patterns for Spring Boot 4.0
    - Replace System.out.println with IO.println

    **Lesson 07 (Spring Security intro):**
    - Verify SecurityFilterChain configuration is compatible with Spring Security 7 (ships with SB 4.0)
    - Replace System.out.println with IO.println
    - Replace @MockBean with @MockitoBean if present

    **Module 11 module.json:**
    - Update description to reference "Spring Boot 4.0" instead of "Spring Boot 3.4"

    For all 7 challenges:
    - Update solution.java: IO.println, Spring Boot 4.0 patterns
    - Update challenge.json instructions if they reference Spring Boot 3.4

    Commit: "feat(02-05): migrate Modules 10-11 to Spring Boot 4.0.x and Java 25"
  </action>
  <verify>
    - `grep -r "System.out.println" content/courses/java/modules/10-web-fundamentals-and-apis/ | wc -l` returns 0
    - `grep -r "System.out.println" content/courses/java/modules/11-spring-boot/ | wc -l` returns 0
    - `grep -ri "Spring Boot 3.4" content/courses/java/modules/11-spring-boot/ | wc -l` returns 0
    - `grep -ri "Spring Boot 4" content/courses/java/modules/11-spring-boot/ | wc -l` returns > 0
    - Module 11 module.json references Spring Boot 4.0
    - All challenge.json files are valid JSON
  </verify>
  <done>Modules 10-11 fully migrated to Spring Boot 4.0.x. All version references updated. Virtual threads noted as default. IO.println used throughout. @MockitoBean where applicable.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Module 12 (Security/Sessions/JWT) to Spring Boot 4.0.x</name>
  <files>
    content/courses/java/modules/12-security-sessions-jwt/**/content/*.md
    content/courses/java/modules/12-security-sessions-jwt/**/challenges/**
  </files>
  <action>
    Module 12 has 5 lessons and 10 challenges. Security configuration is the most version-sensitive area.

    **For all 5 lessons:**
    1. Replace System.out.println with IO.println
    2. Update Spring Security configuration patterns for Spring Security 7 (ships with Spring Boot 4.0.x):
       - SecurityFilterChain bean approach is the same (deprecated WebSecurityConfigurerAdapter was removed in SB 3.0, so if content still references it, REWRITE)
       - Verify CORS, CSRF configuration patterns are current
       - JWT token handling: verify io.jsonwebtoken (jjwt) or Spring Security's built-in OAuth2 resource server patterns
    3. Update any "Spring Boot 3.4" or "Spring Security 6" references to "Spring Boot 4.0" / "Spring Security 7"
    4. Check for any deprecated security configurations:
       - `authorizeRequests()` should be `authorizeHttpRequests()` (changed in Spring Security 6.x, should already be correct but verify)
       - `antMatchers()` should be `requestMatchers()` (same)
       - If using custom UserDetailsService, verify the pattern is current

    **Special attention to JWT implementation:**
    - Verify JWT library imports (io.jsonwebtoken is most common)
    - Verify token generation and validation patterns
    - Ensure password encoding uses BCryptPasswordEncoder (still standard)

    **For all 10 challenges:**
    - Update solution.java: IO.println, correct Spring Security imports
    - Update challenge.json instructions: update version references
    - Verify security configuration challenges use current patterns

    Commit: "feat(02-05): migrate Module 12 security to Spring Boot 4.0.x / Spring Security 7"
  </action>
  <verify>
    - `grep -r "System.out.println" content/courses/java/modules/12-security-sessions-jwt/ | wc -l` returns 0
    - `grep -ri "Spring Boot 3" content/courses/java/modules/12-security-sessions-jwt/ | wc -l` returns 0
    - No references to WebSecurityConfigurerAdapter
    - No references to antMatchers (should use requestMatchers)
    - All challenge.json files are valid JSON
  </verify>
  <done>Module 12 security content fully migrated to Spring Boot 4.0.x / Spring Security 7. All security configurations use current patterns. IO.println throughout.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Zero System.out.println in Modules 10-12
2. All Spring Boot references target 4.0.x (zero references to 3.4)
3. Spring Security patterns compatible with Spring Security 7
4. @MockitoBean used instead of @MockBean
5. All 20 challenges updated
</verification>

<success_criteria>
- JAVA-01 partially satisfied: Modules 10-12 use consistent IO.println and Spring Boot 4.0.x references
- JAVA-02 partially satisfied: Spring Boot modules teach current framework patterns
- Web development path (Modules 10-12) is coherent and version-consistent
</success_criteria>

<output>
After completion, create `.planning/phases/02-java-course-audit/02-05-SUMMARY.md`
</output>
