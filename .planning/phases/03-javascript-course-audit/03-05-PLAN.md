---
phase: 03-javascript-course-audit
plan: 05
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - content/courses/javascript/modules/13-modern-frontend-with-react/**/*.md
  - content/courses/javascript/modules/14-full-stack-integration/**/*.md
  - content/courses/javascript/modules/15-deployment-and-professional-tools/**/*.md
  - content/courses/javascript/modules/16-testing-with-bun/**/*.md
  - content/courses/javascript/modules/16-testing-with-bun/**/challenges/**/*.js
autonomous: true

must_haves:
  truths:
    - "React content uses React 19 patterns (functional components, hooks, no class components as primary)"
    - "Docker images reference oven/bun:1 (not oven/bun:1.1)"
    - "Module 16 testing challenges all have bun:test simulation wrappers for Node.js compatibility"
    - "Full-stack integration patterns are consistent between Modules 13, 14, and 20-21"
    - "Deployment content references current platforms and configurations"
  artifacts:
    - path: ".planning/phases/03-javascript-course-audit/03-05-SUMMARY.md"
      provides: "Accuracy pass results for Modules 13-16"
  key_links:
    - from: "Module 15 Docker content"
      to: "oven/bun Docker images"
      via: "Docker image version"
      pattern: "oven/bun:1[^.]"
    - from: "Module 16 challenge files"
      to: "Node.js execution service"
      via: "bun:test simulation wrappers"
      pattern: "Simulating bun:test"
---

<objective>
Accuracy pass across Modules 13-16 (29 lessons, ~142 content files): verify React 19 patterns, full-stack integration architecture, deployment configurations (fix stale Docker images), and testing module content. Ensure all Module 16 testing challenges have bun:test simulation wrappers for Node.js compatibility.

Purpose: These modules bridge the gap from individual technologies (TS, Hono, Prisma) to integrated applications. React 19 accuracy matters because students will build their capstone with it. Docker image staleness is a deployment blocker. Testing challenges must actually execute in the app.

Output: Corrected content across 4 modules with Docker images updated and test simulation wrappers verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-javascript-course-audit/03-RESEARCH.md
@.planning/phases/03-javascript-course-audit/03-01-SUMMARY.md
@.planning/phases/03-javascript-course-audit/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accuracy pass Modules 13-15 (React + Integration + Deployment)</name>
  <files>
    content/courses/javascript/modules/13-modern-frontend-with-react/**/*.md
    content/courses/javascript/modules/14-full-stack-integration/**/*.md
    content/courses/javascript/modules/15-deployment-and-professional-tools/**/*.md
  </files>
  <action>
    Read every content file in Modules 13-15 (19 lessons). Verify:

    **1. Module 13 -- React:**
    - All components use functional component syntax (no class components as primary examples)
    - Hooks: useState, useEffect, useContext, useReducer, useMemo, useCallback -- verify correct usage patterns
    - React 19 specific: verify if content mentions React Compiler, use() hook, or Server Components. If mentioned, verify accuracy. If not mentioned, this is fine (React 19 is backward compatible)
    - JSX syntax is correct
    - Vite mentioned as the build tool (not Create React App, which is deprecated)
    - Tailwind CSS: if used, verify class names are current Tailwind v3/v4 syntax
    - react-router-dom: verify routing patterns match v6+ (useNavigate, useParams, <Route element={}>, no <Switch>)

    **2. Module 14 -- Full-Stack Integration:**
    - CORS setup: verify correct pattern for Hono CORS middleware
    - API client patterns: verify fetch() usage with proper error handling
    - Environment variables: verify process.env usage is correct for Node.js, Bun.env for Bun
    - Architecture: verify the separation between frontend (React/Vite) and backend (Hono/Bun) is clearly explained

    **3. Module 15 -- Deployment (CRITICAL Docker fix):**
    - **Docker images:** Research found multiple references to `oven/bun:1.1` and `oven/bun:1.1-slim`. These MUST be updated to `oven/bun:1` (or `oven/bun:latest`). The specific files are:
      - `lessons/06-docker-for-javascript-apps-the-shipping-container-analogy/content/02-example.md`
      - `lessons/06-docker-for-javascript-apps-the-shipping-container-analogy/content/03-theory.md`
      - `lessons/06-docker-for-javascript-apps-the-shipping-container-analogy/content/04-warning.md`
      - `lessons/06-docker-for-javascript-apps-the-shipping-container-analogy/challenges/01-practice-challenge/starter.js`
      - `lessons/06-docker-for-javascript-apps-the-shipping-container-analogy/challenges/01-practice-challenge/solution.js`
    - Update ALL `oven/bun:1.1` -> `oven/bun:1` and `oven/bun:1.1-slim` -> `oven/bun:1-slim`
    - Git/GitHub: verify Git content is accurate and current
    - CI/CD: verify GitHub Actions or equivalent CI patterns are current
    - Environment variables: verify .env handling best practices

    **4. Fix any inaccuracies.**

    Commit: "fix(03-05): accuracy pass Modules 13-15 -- React 19, Docker oven/bun:1, deployment config"
  </action>
  <verify>
    - `grep -r "oven/bun:1.1" content/courses/javascript/ | wc -l` returns 0 (all updated to oven/bun:1)
    - `grep -r "oven/bun:1" content/courses/javascript/ | head -10` shows only oven/bun:1 or oven/bun:1-slim (no 1.1)
    - No class component syntax in Module 13 (except if explicitly teaching legacy patterns)
    - react-router-dom uses v6+ patterns (no <Switch>)
    - Vite referenced as build tool (no Create React App references)
  </verify>
  <done>Modules 13-15 verified. React 19 patterns confirmed. Docker images updated to oven/bun:1. Deployment configurations current.</done>
</task>

<task type="auto">
  <name>Task 2: Accuracy pass Module 16 (Testing) + simulation wrapper verification</name>
  <files>
    content/courses/javascript/modules/16-testing-with-bun/**/*.md
    content/courses/javascript/modules/16-testing-with-bun/**/challenges/**/*.js
  </files>
  <action>
    Read every content file and challenge file in Module 16 (10 lessons, 14 challenges). Verify:

    **1. Testing content accuracy:**
    - bun:test API: describe(), it()/test(), expect() -- verify against current Bun test runner docs
    - Assertion methods: toBe(), toEqual(), toContain(), toThrow(), toBeTruthy(), toBeFalsy() -- verify all exist in bun:test
    - Mocking: mock(), spyOn() -- verify bun:test mocking API
    - Async testing patterns: async test functions, expect().resolves, expect().rejects
    - Integration testing with Hono: app.request() or similar pattern

    **2. Challenge simulation wrapper verification (CRITICAL):**
    Per research, Module 16 already uses a simulation wrapper pattern for bun:test. Verify that EVERY challenge file (starter.js and solution.js) has the simulation wrapper at the top. The pattern is:
    ```javascript
    // Simulating bun:test functions for this exercise
    const describe = (name, fn) => { ... };
    const it = (name, fn) => { ... };
    const expect = (val) => ({ ... });
    ```

    For each of the 14 challenge pairs (starter.js + solution.js = 28 files):
    - If the wrapper EXISTS: verify it is functional and includes all needed assertion methods
    - If the wrapper is MISSING: add it (following the established pattern from Module 16 Lesson 02)
    - Verify the challenge actually RUNS in Node.js by checking for any remaining `import { ... } from 'bun:test'` statements that would cause import errors

    **Ensure simulation wrappers include these methods at minimum:**
    - describe(name, fn)
    - it(name, fn) / test(name, fn)
    - expect(val).toBe(expected)
    - expect(val).toEqual(expected)
    - expect(fn).toThrow()
    - If a specific challenge uses additional assertions (toContain, toBeTruthy, etc.), add those to the wrapper

    **3. Content accuracy for testing patterns:**
    - AAA pattern (Arrange-Act-Assert) described correctly
    - Test organization (describe blocks, nested describes) accurate
    - Coverage: if `bun test --coverage` is mentioned, verify it is a real Bun feature
    - Test file naming: verify conventions (.test.js or .spec.js patterns)

    **4. Fix any inaccuracies and missing wrappers.**

    Commit: "fix(03-05): accuracy pass Module 16 -- testing patterns, simulation wrapper verification"
  </action>
  <verify>
    - `grep -rn "from 'bun:test'" content/courses/javascript/modules/16-*/challenges/ | wc -l` shows zero RAW bun:test imports in challenge files (all should use simulation)
    - `grep -rn "Simulating bun:test" content/courses/javascript/modules/16-*/challenges/ | wc -l` shows wrappers present in challenge files
    - All 28 challenge files (14 starter + 14 solution) have been verified
  </verify>
  <done>Module 16 testing content verified. All 14 challenges have bun:test simulation wrappers and will execute in Node.js. Testing patterns and API references accurate.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 29 lessons in Modules 13-16 reviewed for accuracy
2. Docker images updated from oven/bun:1.1 to oven/bun:1
3. React 19 patterns verified (functional components, hooks, Vite)
4. All Module 16 challenges have Node.js-compatible simulation wrappers
5. No deprecated or phantom APIs remain
</verification>

<success_criteria>
- JSCR-01 partially satisfied: Modules 13-16 (29/132 lessons) verified
- JSCR-03 partially satisfied: Module 16 challenges verified for Node.js execution
- Docker deployment content is current
- Testing challenges are app-compatible
</success_criteria>

<output>
After completion, create `.planning/phases/03-javascript-course-audit/03-05-SUMMARY.md`

The SUMMARY must include:
- Count of files modified per module
- Docker image updates (file list)
- React patterns verified/corrected
- Simulation wrapper status for each of 14 Module 16 challenges
- Any issues deferred to later plans
</output>
