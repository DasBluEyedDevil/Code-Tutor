---
phase: 03-javascript-course-audit
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - content/courses/javascript/modules/10-typescript-fundamentals/**/*.md
  - content/courses/javascript/modules/11-server-with-bun-and-hono/**/*.md
  - content/courses/javascript/modules/12-databases-and-prisma-orm/**/*.md
autonomous: true

must_haves:
  truths:
    - "TypeScript content targets TS 5.x with no references to TS 7.0 as released"
    - "Hono middleware examples include alg option in jwt() calls (required since 4.11.0)"
    - "Prisma content uses 6.x patterns with note about 7.x availability"
    - "Bun server setup instructions are compatible with Bun 1.3.x"
    - "No phantom APIs or deprecated patterns in Modules 10-12"
  artifacts:
    - path: ".planning/phases/03-javascript-course-audit/03-04-SUMMARY.md"
      provides: "Accuracy pass results for Modules 10-12"
  key_links:
    - from: "Module 11 Hono JWT middleware"
      to: "Hono 4.11.0 API"
      via: "alg option requirement"
      pattern: "jwt\\(.*alg"
    - from: "Module 12 Prisma schema"
      to: "Prisma 6.x API"
      via: "Generator and client patterns"
      pattern: "prisma|@prisma/client"
---

<objective>
Accuracy pass across Modules 10-12 (24 lessons, ~131 content files): verify TypeScript fundamentals against TS 5.x, Hono/Bun server content against Hono 4.11+/Bun 1.3.x, and Prisma ORM content against Prisma 6.x. Fix the TypeScript 7.0 misinformation (it is in Module 17, but the TS version context applies here too). Verify Hono JWT middleware has the now-required `alg` option.

Purpose: These three modules form the backend technology stack taught in the course. TypeScript typing accuracy matters because it underpins the Hono and Prisma modules. Hono JWT security is a real-world concern. Prisma version clarity prevents student confusion.

Output: Corrected content across 3 modules with all framework APIs verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-javascript-course-audit/03-RESEARCH.md
@.planning/phases/03-javascript-course-audit/03-01-SUMMARY.md
@.planning/phases/03-javascript-course-audit/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accuracy pass Module 10 (TypeScript Fundamentals)</name>
  <files>
    content/courses/javascript/modules/10-typescript-fundamentals/**/*.md
  </files>
  <action>
    Read every content file in Module 10 (10 lessons, ~53 content files). Verify:

    **1. TypeScript version accuracy:**
    - All content should reference TypeScript 5.x as current stable
    - NoInfer<T> utility type (TS 5.4+) -- verify it is described correctly
    - `satisfies` operator (TS 4.9+) -- verify syntax and use case accuracy
    - Decorators: if mentioned, clarify they are available via `experimentalDecorators` flag or the new TC39 decorators (TS 5.0+)
    - Template literal types, conditional types, mapped types -- verify syntax accuracy

    **2. Type system accuracy:**
    - `unknown` vs `any` -- correctly distinguished
    - `never` type -- correctly explained
    - Union types, intersection types -- correct syntax
    - Generic constraints -- correct `extends` keyword usage
    - Utility types (Partial, Required, Pick, Omit, Record, Exclude, Extract, ReturnType) -- verify signatures match TS 5.x docs

    **3. TS + JS integration:**
    - `tsconfig.json` examples should use current recommended settings
    - `strict: true` should be the default recommendation
    - Module resolution: verify `"moduleResolution": "bundler"` or `"node16"` is recommended (not `"node"` which is legacy)

    **4. Fix any inaccuracies.** Do NOT add new content sections.

    Commit: "fix(03-04): accuracy pass Module 10 -- TypeScript 5.x verification"
  </action>
  <verify>
    - No content claims TypeScript 7.0 is released (may appear in Module 17, but check M10 too)
    - NoInfer utility type described correctly (TS 5.4+)
    - tsconfig.json examples use current recommended settings
  </verify>
  <done>Module 10 TypeScript content verified against TS 5.x. All utility types, generic patterns, and configuration examples are accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Accuracy pass Modules 11-12 (Bun/Hono + Prisma)</name>
  <files>
    content/courses/javascript/modules/11-server-with-bun-and-hono/**/*.md
    content/courses/javascript/modules/12-databases-and-prisma-orm/**/*.md
  </files>
  <action>
    Read every content file in Modules 11-12 (14 lessons, ~78 content files). Verify:

    **1. Module 11 -- Hono/Bun Server (CRITICAL):**
    - **JWT middleware `alg` requirement:** Search for `jwt(` in all content. Since Hono 4.11.0, the `alg` option is REQUIRED in `jwt()` and `jwk()` middleware. Research found instances like:
      ```
      app.use('/api/protected/*', jwt({ secret: 'your-secret' }));
      ```
      This MUST be updated to:
      ```
      app.use('/api/protected/*', jwt({ secret: 'your-secret', alg: 'HS256' }));
      ```
      Fix ALL instances.
    - Hono routing: verify `app.get()`, `app.post()`, `app.put()`, `app.delete()` patterns match Hono 4.x API
    - Hono context object: verify `c.req.param()`, `c.req.query()`, `c.json()`, `c.text()` are current API
    - Zod validation: verify Zod import and usage patterns are current (`z.object()`, `z.string()`, `.parse()`, `.safeParse()`)
    - Bun server: verify `Bun.serve()` or `export default app` patterns for Hono on Bun
    - Middleware patterns: verify `app.use()` and custom middleware function signatures

    **2. Module 12 -- Prisma ORM:**
    - Schema syntax: verify `generator client { provider = "prisma-client-js" }` is the Prisma 6.x pattern
    - Prisma Client usage: verify `prisma.model.findMany()`, `prisma.model.create()`, etc. match Prisma 6.x API
    - Relations: verify `@relation` syntax is correct
    - Migrations: verify `npx prisma migrate dev` workflow is current
    - If Prisma 7 is mentioned anywhere, ensure it is described as "released but course uses 6.x" with brief note on key differences (ESM-first, no Rust engines)
    - If Prisma 7 is NOT mentioned, add a brief note in an appropriate WARNING section stating Prisma 7.x has been released with breaking changes and the course content uses 6.x patterns

    **3. Version-specific patterns:**
    - `bunx prisma` vs `npx prisma` -- Bun uses `bunx` as its npx equivalent. Verify consistency.
    - Database connection strings -- verify examples use standard format
    - Prisma Studio: verify `npx prisma studio` / `bunx prisma studio` is documented

    **4. Fix any inaccuracies.**

    Commit: "fix(03-04): accuracy pass Modules 11-12 -- Hono JWT alg fix, Prisma 6.x verification"
  </action>
  <verify>
    - `grep -r "jwt(" content/courses/javascript/modules/11-* --include="*.md"` shows `alg` parameter in every jwt() call
    - Prisma 6.x patterns used throughout Module 12
    - Hono 4.x API methods verified (no phantom methods)
    - Zod validation patterns are syntactically correct
  </verify>
  <done>Modules 11-12 verified. Hono JWT middleware updated with required alg option. Prisma 6.x patterns confirmed with 7.x availability noted. All server API patterns match current stable versions.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 24 lessons in Modules 10-12 reviewed for accuracy
2. TypeScript 5.x features correctly described
3. Hono JWT middleware has alg parameter everywhere
4. Prisma 6.x patterns used with 7.x note
5. No phantom APIs or deprecated patterns remain
</verification>

<success_criteria>
- JSCR-01 partially satisfied: Modules 10-12 (24/132 lessons) verified
- Hono security fix applied (jwt alg parameter)
- Prisma version strategy documented in content
</success_criteria>

<output>
After completion, create `.planning/phases/03-javascript-course-audit/03-04-SUMMARY.md`

The SUMMARY must include:
- Count of files modified per module
- Hono JWT alg fixes applied (file list)
- Prisma version note added (where)
- TypeScript accuracy corrections
- Any issues deferred to later plans
</output>
