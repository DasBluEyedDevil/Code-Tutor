# NPM Configuration for optimized installs

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Save exact versions for reproducibility
save-exact=true

# Strict SSL
strict-ssl=true

# Prefer offline when possible
prefer-offline=true

