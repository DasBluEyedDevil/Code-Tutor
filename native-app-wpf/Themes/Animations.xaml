<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================== -->
    <!-- EASING FUNCTIONS - Smooth, Professional Motion -->
    <!-- ============================================== -->

    <!-- Primary easing for most UI animations (QuadraticEaseOut) -->
    <QuadraticEase x:Key="EaseOut" EasingMode="EaseOut" />
    <QuadraticEase x:Key="EaseIn" EasingMode="EaseIn" />
    <QuadraticEase x:Key="EaseInOut" EasingMode="EaseInOut" />

    <!-- Snappy easing for micro-interactions -->
    <CubicEase x:Key="SnapEaseOut" EasingMode="EaseOut" />
    <CubicEase x:Key="SnapEaseIn" EasingMode="EaseIn" />

    <!-- Bouncy easing for success/celebration moments -->
    <ElasticEase x:Key="BounceEase" EasingMode="EaseOut" Oscillations="1" Springiness="8" />
    <BackEase x:Key="OvershootEase" EasingMode="EaseOut" Amplitude="0.3" />

    <!-- Smooth exponential for premium feel -->
    <ExponentialEase x:Key="SmoothEase" EasingMode="EaseOut" Exponent="4" />

    <!-- ============================================== -->
    <!-- ANIMATION DURATIONS - Consistent Timing -->
    <!-- ============================================== -->

    <Duration x:Key="DurationInstant">0:0:0.1</Duration>
    <Duration x:Key="DurationFast">0:0:0.15</Duration>
    <Duration x:Key="DurationNormal">0:0:0.25</Duration>
    <Duration x:Key="DurationSlow">0:0:0.4</Duration>
    <Duration x:Key="DurationPageTransition">0:0:0.3</Duration>

    <!-- ============================================== -->
    <!-- CARD ENTRANCE ANIMATION - Slide Up + Fade In -->
    <!-- ============================================== -->

    <Storyboard x:Key="CardEntranceStoryboard">
        <!-- Fade In -->
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.35">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>

        <!-- Slide Up from 30px below -->
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
            From="30" To="0"
            Duration="0:0:0.35">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ============================================== -->
    <!-- PAGE TRANSITION ANIMATIONS -->
    <!-- ============================================== -->

    <!-- Fade + Slide Left (for navigating forward) -->
    <Storyboard x:Key="PageSlideInFromRight">
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
            From="60" To="0"
            Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- Fade + Slide Right (for navigating back) -->
    <Storyboard x:Key="PageSlideInFromLeft">
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
            From="-60" To="0"
            Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- Fade + Scale Up (for modals/dialogs) -->
    <Storyboard x:Key="PageScaleIn">
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
            From="0.95" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
            From="0.95" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ============================================== -->
    <!-- SUCCESS FLASH ANIMATION - Green pulse + Glow -->
    <!-- ============================================== -->

    <Storyboard x:Key="SuccessFlashStoryboard">
        <!-- Flash the border green then back -->
        <ColorAnimation
            Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
            To="#3FB950"
            Duration="0:0:0.15"
            AutoReverse="True">
            <ColorAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut" />
            </ColorAnimation.EasingFunction>
        </ColorAnimation>

        <!-- Subtle scale pop -->
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
            From="1" To="1.02"
            Duration="0:0:0.15"
            AutoReverse="True">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
            From="1" To="1.02"
            Duration="0:0:0.15"
            AutoReverse="True">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ============================================== -->
    <!-- SUCCESS POP TEXT ANIMATION -->
    <!-- ============================================== -->

    <Storyboard x:Key="SuccessPopStoryboard">
        <!-- Scale 0 -> 1.2 -> 1.0 with overshoot -->
        <DoubleAnimationUsingKeyFrames
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)">
            <EasingDoubleKeyFrame KeyTime="0:0:0" Value="0" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="1.15">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
            <EasingDoubleKeyFrame KeyTime="0:0:0.35" Value="1">
                <EasingDoubleKeyFrame.EasingFunction>
                    <QuadraticEase EasingMode="EaseInOut" />
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
        <DoubleAnimationUsingKeyFrames
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)">
            <EasingDoubleKeyFrame KeyTime="0:0:0" Value="0" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="1.15">
                <EasingDoubleKeyFrame.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
            <EasingDoubleKeyFrame KeyTime="0:0:0.35" Value="1">
                <EasingDoubleKeyFrame.EasingFunction>
                    <QuadraticEase EasingMode="EaseInOut" />
                </EasingDoubleKeyFrame.EasingFunction>
            </EasingDoubleKeyFrame>
        </DoubleAnimationUsingKeyFrames>
        <!-- Fade in -->
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.15" />
    </Storyboard>

    <!-- ============================================== -->
    <!-- LOADING SPINNER ANIMATION -->
    <!-- ============================================== -->

    <Storyboard x:Key="SpinnerRotateStoryboard" RepeatBehavior="Forever">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
            From="0" To="360"
            Duration="0:0:1" />
    </Storyboard>

    <!-- ============================================== -->
    <!-- PULSE GLOW ANIMATION (for active elements) -->
    <!-- ============================================== -->

    <Storyboard x:Key="PulseGlowStoryboard" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)"
            From="10" To="20"
            Duration="0:0:1.5">
            <DoubleAnimation.EasingFunction>
                <SineEase EasingMode="EaseInOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.Opacity)"
            From="0.6" To="0.9"
            Duration="0:0:1.5">
            <DoubleAnimation.EasingFunction>
                <SineEase EasingMode="EaseInOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ============================================== -->
    <!-- FADE IN/OUT UTILITY ANIMATIONS -->
    <!-- ============================================== -->

    <Storyboard x:Key="FadeInStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="FadeOutStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="1" To="0"
            Duration="0:0:0.2">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseIn" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ============================================== -->
    <!-- SLIDE PANEL ANIMATIONS (for output panels) -->
    <!-- ============================================== -->

    <Storyboard x:Key="SlideDownExpandStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <QuadraticEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
            From="-15" To="0"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ============================================== -->
    <!-- SHAKE ANIMATION (for errors) -->
    <!-- ============================================== -->

    <Storyboard x:Key="ShakeStoryboard">
        <DoubleAnimationUsingKeyFrames
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
            <EasingDoubleKeyFrame KeyTime="0:0:0" Value="0" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.05" Value="-8" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="8" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="-6" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="6" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="-3" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="0" />
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>

</ResourceDictionary>
