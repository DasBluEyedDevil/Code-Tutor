<UserControl x:Class="CodeTutor.Wpf.Views.LessonPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Lesson Header -->
        <Border Grid.Row="0"
                Background="{StaticResource BackgroundMediumBrush}"
                Padding="40,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <!-- Breadcrumb Navigation -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Button x:Name="BreadcrumbCourse"
                                Style="{StaticResource GhostButton}"
                                Click="BreadcrumbCourse_Click"
                                Padding="0">
                            <TextBlock x:Name="BreadcrumbCourseText"
                                       Style="{StaticResource CaptionText}"
                                       Foreground="{StaticResource AccentBlueBrush}" />
                        </Button>
                        <TextBlock Text=" / "
                                   Style="{StaticResource CaptionText}"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock x:Name="BreadcrumbModuleText"
                                   Style="{StaticResource CaptionText}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <TextBlock x:Name="LessonTitle"
                               Style="{StaticResource HeadingText}" />
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock x:Name="LessonTime"
                                   Style="{StaticResource CaptionText}" />
                        <TextBlock x:Name="LessonDifficulty"
                                   Style="{StaticResource CaptionText}"
                                   Margin="16,0,0,0" />
                    </StackPanel>
                    <TextBlock x:Name="LessonPosition"
                               Style="{StaticResource CaptionText}"
                               Foreground="{StaticResource TextMutedBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <!-- AI Tutor Toggle Button -->
                <Button x:Name="TutorToggleButton"
                        Grid.Column="1"
                        Content="&#xE716;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="18"
                        ToolTip="AI Tutor"
                        Background="Transparent"
                        Foreground="{StaticResource AccentPurpleBrush}"
                        BorderThickness="0"
                        Padding="12,8"
                        Cursor="Hand"
                        VerticalAlignment="Top"
                        Click="TutorToggleButton_Click"/>
            </Grid>
        </Border>

        <!-- Lesson Content -->
        <ScrollViewer Grid.Row="1"
                      Style="{StaticResource DarkScrollViewer}"
                      Padding="40,32">
            <StackPanel x:Name="ContentPanel" MaxWidth="800">
                <!-- Content sections will be added dynamically -->
            </StackPanel>
        </ScrollViewer>

        <!-- Footer Navigation -->
        <Border Grid.Row="2"
                Background="{StaticResource BackgroundMediumBrush}"
                Padding="40,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button x:Name="PrevButton"
                        Grid.Column="0"
                        Style="{StaticResource SecondaryButton}"
                        Click="PrevButton_Click"
                        HorizontalAlignment="Left"
                        IsEnabled="False">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&lt;- " />
                        <TextBlock Text="Previous" />
                    </StackPanel>
                </Button>

                <Button x:Name="CompleteButton"
                        Grid.Column="1"
                        Style="{StaticResource PrimaryButton}"
                        Click="CompleteButton_Click">
                    <TextBlock Text="Mark Complete" />
                </Button>

                <Button x:Name="NextButton"
                        Grid.Column="2"
                        Style="{StaticResource PrimaryButton}"
                        Click="NextButton_Click"
                        HorizontalAlignment="Right"
                        IsEnabled="False">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Next" />
                        <TextBlock Text=" -&gt;" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Tutor Chat Panel (slides in from right) -->
        <Border x:Name="TutorPanel"
                Grid.Column="1"
                Grid.RowSpan="3"
                Width="0"
                Opacity="0"
                Background="{StaticResource BackgroundDarkBrush}"
                BorderBrush="{StaticResource BorderDefaultBrush}"
                BorderThickness="1,0,0,0"
                RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <TranslateTransform X="24" />
            </Border.RenderTransform>
            <ContentPresenter x:Name="TutorContent"/>
        </Border>
    </Grid>
</UserControl>
