<UserControl x:Class="CodeTutor.Wpf.Controls.LegacyComparisonSection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit">

    <Border Style="{StaticResource CardBorder}"
            Background="{StaticResource BackgroundMediumBrush}"
            Margin="0,0,0,24">
        <Expander IsExpanded="False">
            <Expander.Header>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="HeaderIcon"
                               Text="&#x1F4E6; "
                               FontSize="16"
                               VerticalAlignment="Center" />
                    <TextBlock x:Name="HeaderTitle"
                               Style="{StaticResource SubheadingText}"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Expander.Header>

            <Expander.Style>
                <Style TargetType="Expander">
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Expander">
                                <Border Background="Transparent">
                                    <StackPanel>
                                        <ToggleButton x:Name="HeaderSite"
                                                      IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                      Cursor="Hand">
                                            <ToggleButton.Template>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="Transparent" Padding="0,4">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <!-- Collapsed arrow (right pointing) -->
                                                            <TextBlock x:Name="ArrowCollapsed"
                                                                       Text="&#x25B6;"
                                                                       FontSize="10"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       VerticalAlignment="Center"
                                                                       Margin="0,0,8,0"
                                                                       Visibility="Visible" />
                                                            <!-- Expanded arrow (down pointing) -->
                                                            <TextBlock x:Name="ArrowExpanded"
                                                                       Text="&#x25BC;"
                                                                       FontSize="10"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       VerticalAlignment="Center"
                                                                       Margin="0,0,8,0"
                                                                       Visibility="Collapsed" />
                                                            <ContentPresenter Grid.Column="1"
                                                                              Content="{TemplateBinding Content}"
                                                                              ContentTemplate="{TemplateBinding ContentTemplate}" />
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="ArrowCollapsed" Property="Visibility" Value="Collapsed" />
                                                            <Setter TargetName="ArrowExpanded" Property="Visibility" Value="Visible" />
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="ArrowCollapsed" Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                                            <Setter TargetName="ArrowExpanded" Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </ToggleButton.Template>
                                            <ContentPresenter ContentSource="Header" />
                                        </ToggleButton>
                                        <Border x:Name="ContentSite"
                                                Visibility="Collapsed"
                                                Margin="18,8,0,0">
                                            <ContentPresenter />
                                        </Border>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsExpanded" Value="True">
                                        <Setter TargetName="ContentSite" Property="Visibility" Value="Visible" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Expander.Style>

            <StackPanel>
                <!-- Description Text -->
                <TextBlock x:Name="ContentText"
                           Style="{StaticResource BodyText}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,12" />

                <!-- Code Block -->
                <Border Background="{StaticResource BackgroundDarkBrush}"
                        CornerRadius="6"
                        Padding="4">
                    <avalonEdit:TextEditor x:Name="CodeEditor"
                                            IsReadOnly="True"
                                            FontFamily="{StaticResource MonoFont}"
                                            FontSize="13"
                                            ShowLineNumbers="True"
                                            Background="Transparent"
                                            Foreground="{StaticResource TextPrimaryBrush}"
                                            LineNumbersForeground="{StaticResource TextMutedBrush}"
                                            HorizontalScrollBarVisibility="Auto"
                                            VerticalScrollBarVisibility="Auto" />
                </Border>
            </StackPanel>
        </Expander>
    </Border>
</UserControl>
