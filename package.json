{
  "name": "code-tutor",
  "version": "1.0.0",
  "private": true,
  "description": "Unified multi-language training platform",
  "workspaces": [
    "apps/*",
    "apps/executors/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:executor:python": "npm run dev --workspace=apps/executors/python",
    "dev": "concurrently \"npm:dev:*\"",
    "build": "npm run build --workspaces",
    "build:desktop": "npm run build --workspace=apps/web && npm run build --workspace=apps/desktop",
    "start:desktop": "npm run dev --workspace=apps/desktop",
    "package:desktop": "npm run build:desktop && npm run package --workspace=apps/desktop",
    "dist:desktop": "npm run build:desktop && npm run dist --workspace=apps/desktop",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "concurrently": "^8.2.2",
    "ts-node": "10.9.2",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
