{
  "id": "challenge-6a-4-1",
  "type": "FREE_CODING",
  "title": "Write Migration Files",
  "description": "Write migration files to evolve a Notes app schema from v1 to v3.",
  "difficulty": "intermediate",
  "hints": [
    "Use ALTER TABLE ADD COLUMN for new columns",
    "Set NOT NULL with DEFAULT if column must be required",
    "UPDATE existing data when needed",
    "Create indexes for foreign keys"
  ],
  "testCases": [
    {
      "description": "1.sqm should add updated_at and is_pinned",
      "expectedBehavior": "Both columns added with correct types"
    },
    {
      "description": "2.sqm should create Folder table and link to Note",
      "expectedBehavior": "Folder table created, folder_id added to Note"
    }
  ]
}
