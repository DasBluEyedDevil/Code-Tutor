{
  "id": "challenge-6a-1-1",
  "type": "FREE_CODING",
  "title": "SQL Schema Design",
  "description": "Design a SQL schema for a simple task management app with tasks and categories.",
  "difficulty": "intermediate",
  "hints": [
    "Use INTEGER for boolean (0 = false, 1 = true)",
    "Use INTEGER for dates (store as epoch milliseconds)",
    "Add AUTOINCREMENT to primary keys",
    "Use FOREIGN KEY for relationships"
  ],
  "testCases": [
    {
      "description": "Schema should have Categories and Tasks tables",
      "expectedBehavior": "Both CREATE TABLE statements are present"
    },
    {
      "description": "Tasks should reference Categories",
      "expectedBehavior": "category_id foreign key exists"
    }
  ]
}
