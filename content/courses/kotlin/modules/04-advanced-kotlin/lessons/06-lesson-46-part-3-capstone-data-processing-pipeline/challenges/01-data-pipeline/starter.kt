fun main() {
    val scores = listOf(85, 42, 73, 91, 55, 68)
    // Filter passing scores (>= 60)
    // Count passing students
    // Calculate average of passing scores
    
    // Print "Passing: X students" and "Average: X.X"
}
