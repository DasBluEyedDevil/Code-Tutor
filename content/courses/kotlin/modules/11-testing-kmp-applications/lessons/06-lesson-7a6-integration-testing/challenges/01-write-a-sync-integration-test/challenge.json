{
  "id": "challenge-7a-6-1",
  "title": "Write a Sync Integration Test",
  "description": "Test the sync flow: fetch from API, save to database, expose via repository.",
  "difficulty": "advanced",
  "hints": [
    "Use Ktor MockEngine to simulate API responses",
    "Create real in-memory database for repository",
    "Test both success and error paths",
    "Verify both return value AND database state"
  ],
  "testCases": [
    {
      "description": "All 4 sync scenarios should pass",
      "expectedBehavior": "Add new, update newer, preserve newer local, handle errors"
    }
  ]
}