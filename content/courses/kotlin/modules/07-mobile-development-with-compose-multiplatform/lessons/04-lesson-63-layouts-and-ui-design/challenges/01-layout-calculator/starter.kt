fun calculateLayout(items: List<String>, containerHeight: Int) {
    // Distribute items evenly in the container
    // Print "Item X: y=N, height=N" for each
    TODO("Implement layout calculation")
}

fun main() {
    calculateLayout(listOf("A", "B", "C"), 300)
}
